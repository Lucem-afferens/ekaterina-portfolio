# Критические исправления: SSL и получение SCF полей

## 🔴 Проблема 1: Предупреждение "Опасный сайт" (смешанный контент)

**Причина:** WordPress может генерировать URL без протокола или с HTTP, что вызывает предупреждение о смешанном контенте на HTTPS странице.

**Исправления:**

1. **`functions.php`** - Использование `set_url_scheme()` для принудительного HTTPS:
   ```php
   define( 'EKATERINA_THEME_URI', set_url_scheme( get_template_directory_uri(), 'https' ) );
   ```

2. **Все компоненты с изображениями** - Принудительное использование HTTPS для всех URL изображений:
   - `hero-section.php`
   - `introduction-section.php`
   - `about-section.php`
   - `portfolio-section.php`

3. **Все URL изображений** теперь используют `set_url_scheme( $url, 'https' )` для принудительного HTTPS.

---

## 🔴 Проблема 2: Изменения в SCF полях не отображаются

**Причина:** При использовании `get_template_part()` контекст страницы может теряться, и `get_the_ID()` возвращает 0 или неправильный ID.

**Исправления:**

1. **`template-homepage.php`** - Сохранение ID страницы в глобальной переменной:
   ```php
   global $ekaterina_current_page_id;
   $ekaterina_current_page_id = get_queried_object_id();
   ```

2. **`inc/security.php`** - Улучшенная функция `ekaterina_get_current_page_id()`:
   - Сначала проверяет глобальную переменную `$ekaterina_current_page_id`
   - Затем использует `get_queried_object_id()` (работает в `get_template_part()`)
   - Затем `get_the_ID()`
   - Затем глобальную переменную `$post`
   - Затем `get_queried_object()`

3. **`inc/security.php`** - Исправлена проверка пустых значений:
   - Было: `if ( empty( $value ) )` - неправильно для значения "0"
   - Стало: `if ( $value === null || $value === false || $value === '' )` - правильно

4. **`inc/scf-fields.php`** - Улучшена проверка Repeater полей:
   - Более точная проверка на пустые значения и массивы

---

## ✅ Что исправлено

### Файлы с исправлениями:

1. **`portfolio-theme/functions.php`**
   - Принудительное использование HTTPS для констант темы

2. **`portfolio-theme/inc/security.php`**
   - Улучшена функция `ekaterina_get_current_page_id()`
   - Исправлена проверка пустых значений в `ekaterina_get_scf_field()`

3. **`portfolio-theme/inc/scf-fields.php`**
   - Улучшена проверка Repeater полей

4. **`portfolio-theme/templates/template-homepage.php`**
   - Сохранение ID страницы в глобальной переменной перед вызовом компонентов

5. **`portfolio-theme/template-parts/components/hero-section.php`**
   - Принудительное использование HTTPS для URL изображений

6. **`portfolio-theme/template-parts/components/introduction-section.php`**
   - Принудительное использование HTTPS для URL изображений

7. **`portfolio-theme/template-parts/components/about-section.php`**
   - Принудительное использование HTTPS для URL изображений

8. **`portfolio-theme/template-parts/components/portfolio-section.php`**
   - Принудительное использование HTTPS для всех URL изображений портфолио

---

## 🚀 Что делать дальше

1. **Загрузите обновленные файлы на сервер:**
   - Через FTP/SFTP
   - Или через GitHub Actions (автоматический деплой)

2. **Очистите кеш:**
   - Кеш браузера (Ctrl+F5)
   - Кеш WordPress (если используется плагин кеширования)

3. **Проверьте:**
   - Откройте сайт: `https://ekaterina-shul.ru`
   - Предупреждение "Опасный сайт" должно исчезнуть
   - Измените поле в SCF (например, `hero_name`)
   - Нажмите "Обновить" страницу
   - Откройте главную страницу сайта
   - Изменения должны отобразиться

---

## 🔍 Дополнительная диагностика

Если проблемы сохраняются:

### Для SSL:
1. Проверьте, что WordPress настроен на HTTPS:
   - **Настройки → Общие** → оба URL должны быть `https://ekaterina-shul.ru`
2. Проверьте через DevTools (F12):
   - Вкладка **Console** - не должно быть ошибок о смешанном контенте
   - Вкладка **Network** - все ресурсы должны загружаться по HTTPS

### Для SCF полей:
1. Добавьте временный debug код в `hero-section.php`:
   ```php
   // ВРЕМЕННО для отладки
   echo '<!-- Page ID: ' . $current_page_id . ' -->';
   echo '<!-- Hero Name: ' . $hero_name . ' -->';
   ```
2. Проверьте в исходном коде страницы, что ID правильный
3. Проверьте, что группы полей привязаны к шаблону "Homepage"

---

**Дата исправления:** 2025-01-09
**Версия темы:** 1.0.1

