# Настройка SSL сертификата на Beget

## 🔴 Проблема: Предупреждение "Опасный сайт"

Если браузер показывает предупреждение "Опасный сайт", это означает, что:
1. Сайт работает по HTTP вместо HTTPS
2. SSL сертификат не установлен или неправильно настроен
3. Сайт может быть помечен как опасный в базе Google Safe Browsing

## ✅ Решение: Установка SSL сертификата на Beget

### Шаг 1: Вход в панель управления Beget

1. Войдите в панель управления Beget: https://cp.beget.com
2. Найдите ваш домен `ekaterina-shul.ru`

### Шаг 2: Установка SSL сертификата

**Вариант 1: Бесплатный SSL от Let's Encrypt (рекомендуется)**

1. В панели Beget перейдите: **Домены → SSL-сертификаты**
2. Найдите домен `ekaterina-shul.ru`
3. Нажмите **"Установить SSL"** или **"Получить Let's Encrypt"**
4. Выберите тип: **Let's Encrypt** (бесплатный)
5. Нажмите **"Установить"** или **"Активировать"**
6. Дождитесь установки (обычно 1-5 минут)

**Вариант 2: Автоматическая установка через панель**

1. В панели Beget: **Домены → Управление доменами**
2. Найдите `ekaterina-shul.ru`
3. Нажмите на иконку настроек (шестеренка) рядом с доменом
4. Найдите раздел **"SSL"** или **"Безопасность"**
5. Включите **"Автоматическая установка SSL"** или **"Let's Encrypt"**
6. Сохраните изменения

### Шаг 3: Принудительное перенаправление на HTTPS

После установки SSL нужно настроить WordPress для работы с HTTPS.

**Через панель Beget:**

1. В панели Beget: **Домены → Управление доменами**
2. Найдите `ekaterina-shul.ru`
3. Включите опцию **"Принудительное перенаправление на HTTPS"** или **"Force HTTPS"**
4. Сохраните изменения

**Через WordPress (если опция в панели недоступна):**

1. Войдите в админ-панель WordPress: `https://ekaterina-shul.ru/wp-admin`
2. Перейдите: **Настройки → Общие**
3. Измените:
   - **Адрес WordPress (URL)**: `https://ekaterina-shul.ru`
   - **Адрес сайта (URL)**: `https://ekaterina-shul.ru`
4. Нажмите **"Сохранить изменения"**

### Шаг 4: Настройка через wp-config.php (альтернативный метод)

Если нужно принудительно включить HTTPS:

1. Подключитесь к серверу через FTP или File Manager
2. Откройте файл `wp-config.php` в корне WordPress
3. Добавьте перед строкой `/* That's all, stop editing! */`:

```php
// Принудительное использование HTTPS
define('FORCE_SSL_ADMIN', true);
if (isset($_SERVER['HTTP_X_FORWARDED_PROTO']) && $_SERVER['HTTP_X_FORWARDED_PROTO'] === 'https') {
    $_SERVER['HTTPS'] = 'on';
}
```

4. Сохраните файл

### Шаг 5: Настройка через .htaccess (для Apache)

Если на сервере используется Apache:

1. Подключитесь через FTP или File Manager
2. Откройте файл `.htaccess` в корне WordPress
3. Добавьте в начало файла (после строки `# BEGIN WordPress`):

```apache
# Принудительное перенаправление на HTTPS
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
</IfModule>
```

4. Сохраните файл

**⚠️ ВАЖНО:** Не удаляйте существующие правила WordPress в `.htaccess`!

## 🔍 Проверка SSL сертификата

### После установки SSL проверьте:

1. **Откройте сайт:** `https://ekaterina-shul.ru`
2. **Проверьте в браузере:**
   - Должен быть замочек 🔒 в адресной строке
   - URL должен начинаться с `https://`
   - Не должно быть предупреждений о безопасности

3. **Проверьте через онлайн-сервисы:**
   - https://www.ssllabs.com/ssltest/ (SSL Labs)
   - https://www.sslshopper.com/ssl-checker.html (SSL Shopper)

## 🛠️ Решение проблем

### Проблема: SSL установлен, но сайт все еще показывает предупреждение

**Решение:**
1. Очистите кеш браузера (Ctrl+Shift+Delete)
2. Проверьте, что WordPress настроен на HTTPS (Настройки → Общие)
3. Убедитесь, что включено принудительное перенаправление на HTTPS
4. Проверьте, что все ссылки на сайте используют HTTPS (не смешанный контент)

### Проблема: Смешанный контент (Mixed Content)

Если на странице есть ресурсы, загружаемые по HTTP (изображения, скрипты, стили), браузер может показывать предупреждение.

**Решение:**
1. Установите плагин **"SSL Insecure Content Fixer"** или **"Really Simple SSL"**
2. Или вручную замените все HTTP ссылки на HTTPS в контенте

### Проблема: SSL сертификат не устанавливается

**Решение:**
1. Убедитесь, что домен правильно привязан к хостингу
2. Проверьте DNS записи (A-запись должна указывать на IP сервера Beget)
3. Обратитесь в поддержку Beget: support@beget.com

### Проблема: Сайт помечен как опасный в Google Safe Browsing

**Решение:**
1. Проверьте сайт через: https://transparencyreport.google.com/safe-browsing/search
2. Если сайт помечен как опасный:
   - Проверьте сайт на вирусы и вредоносный код
   - Удалите вредоносный контент
   - Запросите повторную проверку через Google Search Console

## 📝 Чек-лист после настройки SSL

- [ ] SSL сертификат установлен в панели Beget
- [ ] Сайт открывается по HTTPS (https://ekaterina-shul.ru)
- [ ] В адресной строке браузера есть замочек 🔒
- [ ] WordPress настроен на HTTPS (Настройки → Общие)
- [ ] Включено принудительное перенаправление на HTTPS
- [ ] Нет предупреждений о безопасности в браузере
- [ ] Все ресурсы (изображения, CSS, JS) загружаются по HTTPS
- [ ] Сайт проверен через SSL Labs (оценка A или выше)

## 🔗 Полезные ссылки

- **Панель управления Beget:** https://cp.beget.com
- **Поддержка Beget:** support@beget.com
- **Проверка SSL:** https://www.ssllabs.com/ssltest/
- **Google Safe Browsing:** https://transparencyreport.google.com/safe-browsing/search
- **Google Search Console:** https://search.google.com/search-console

## ⚠️ Важные замечания

1. **После установки SSL** может потребоваться время (до 24 часов) для обновления кеша браузеров
2. **Проверьте все ссылки** на сайте - они должны использовать HTTPS
3. **Обновите внешние ссылки** (если сайт упоминается где-то еще)
4. **Проверьте социальные сети** - обновите ссылки на сайт на HTTPS

---

**После выполнения всех шагов предупреждение "Опасный сайт" должно исчезнуть.**

