# Устранение проблем со стилями

## 🔴 Проблемы, которые были исправлены

### 1. Фиолетовый цвет ссылки в header-logo
**Проблема:** Ссылка внутри `.header-logo` имела фиолетовый цвет (стандартный стиль браузера).

**Причина:** В CSS не было стилей для `.header-logo a`, поэтому применялись стандартные стили браузера.

**Решение:** Добавлены стили:
```css
.header-logo a {
    color: #f8f6f3;
    text-decoration: none;
    transition: color 0.3s;
}

.header-logo a:hover {
    color: #9d7e5f;
}
```

### 2. Глобальный сброс стилей ссылок
**Проблема:** Стандартные стили браузера могли переопределять кастомные стили.

**Решение:** Добавлен глобальный сброс в начале CSS:
```css
a {
    color: inherit;
    text-decoration: none;
}
```

---

## ⚠️ Если стили все еще не применяются

### 1. Очистка кеша браузера
- **Windows/Linux:** `Ctrl + F5` или `Ctrl + Shift + R`
- **Mac:** `Cmd + Shift + R`
- Или откройте DevTools (F12) → вкладка Network → включите "Disable cache"

### 2. Очистка кеша WordPress
Если используется плагин кеширования (WP Super Cache, W3 Total Cache, и т.д.):
- Перейдите в настройки плагина
- Нажмите "Очистить кеш" или "Purge Cache"
- Или временно отключите плагин кеширования

### 3. Проверка загрузки CSS файла
1. Откройте сайт в браузере
2. Нажмите F12 (DevTools)
3. Перейдите на вкладку "Network"
4. Обновите страницу (F5)
5. Найдите файл `main.*.css` в списке
6. Проверьте:
   - Статус должен быть `200` (OK)
   - Размер файла должен быть ~28-29 KB
   - Время загрузки должно быть нормальным

### 4. Проверка версии CSS файла
WordPress использует версию файла для кеширования. Проверьте в `enqueue-assets.php`:
- Используется `filemtime()` для версии файла
- При изменении файла версия должна обновиться автоматически

### 5. Проверка конфликтов с другими стилями
1. Откройте DevTools (F12)
2. Выберите элемент (например, `.header-logo a`)
3. Посмотрите вкладку "Computed" или "Styles"
4. Проверьте, какие стили применяются
5. Если видите зачеркнутые стили, значит они переопределяются другими правилами

### 6. Проверка приоритета стилей
Если стили переопределяются, можно использовать `!important` (но это крайняя мера):
```css
.header-logo a {
    color: #f8f6f3 !important;
}
```

---

## 🔍 Диагностика проблем

### Проверка, загружается ли правильный CSS файл
1. Откройте исходный код страницы (Ctrl+U)
2. Найдите `<link rel="stylesheet"` для основного CSS
3. Проверьте путь: должен быть `/wp-content/themes/portfolio-theme/assets/css/main.*.css`
4. Откройте этот URL в браузере
5. Проверьте, что файл содержит ваши стили

### Проверка через консоль браузера
Откройте консоль (F12 → Console) и выполните:
```javascript
// Проверка загруженных стилей
Array.from(document.styleSheets).forEach(sheet => {
    try {
        if (sheet.href && sheet.href.includes('main')) {
            console.log('CSS файл:', sheet.href);
            console.log('Правила:', sheet.cssRules ? sheet.cssRules.length : 'недоступно');
        }
    } catch(e) {
        console.log('Ошибка доступа к стилям:', e);
    }
});
```

---

## 📝 Что было изменено

1. **Добавлены стили для `.header-logo a`**
   - Цвет: `#f8f6f3`
   - Hover эффект: `#9d7e5f`
   - Убрано подчеркивание

2. **Добавлен глобальный сброс стилей ссылок**
   - Все ссылки теперь наследуют цвет от родителя
   - Убрано подчеркивание по умолчанию

3. **Сборка CSS обновлена**
   - Новый файл: `main.CcYUdv1E.css` (или другой хеш)
   - Размер: ~28.81 KB

---

## ✅ После исправления

1. **Загрузите обновленные файлы на сервер**
   - Через GitHub Actions (автоматически)
   - Или вручную через FTP

2. **Очистите кеш:**
   - Браузер (Ctrl+F5)
   - WordPress (если используется плагин)

3. **Проверьте:**
   - Ссылка в header-logo должна быть белого цвета
   - При наведении должна становиться `#9d7e5f`
   - Все остальные стили должны работать корректно

---

**Дата исправления:** 2025-01-09  
**Версия темы:** 1.0.3

