# Решение проблем с SCF полями

## ❓ Вопрос 1: Почему некоторые поля повторяются?

### Разница между полями Contact Section и Theme Options

**Contact Section** (группа `contact_section`):
- Привязка: **Главная страница** (Homepage Template)
- Используется: Только в секции "Обсудим ваше мероприятие" на главной странице
- Поля: `contact_phone`, `contact_email`, `contact_location`, `contact_vk_link`, `contact_telegram_link`, `contact_whatsapp_link`

**Theme Options** (группа `theme_options`):
- Привязка: **Options Page** (глобальные настройки темы)
- Используется: В шапке сайта (header), подвале (footer), и других местах, где нужны общие данные
- Поля: `site_host_name`, `site_host_title`, `site_phone`, `site_email`, `site_location`, `site_vk`, `site_telegram`, `site_whatsapp`

### Зачем нужны оба набора полей?

1. **Contact Section** - для конкретной секции на главной странице
   - Может содержать специфичную информацию для этой секции
   - Можно изменить только для главной страницы, не затрагивая другие места

2. **Theme Options** - для глобальных настроек
   - Используется в header (имя в логотипе)
   - Используется в footer (имя и должность)
   - Может использоваться в других местах сайта

### Рекомендация

**Вариант 1 (Рекомендуется):** Использовать оба набора
- В Contact Section заполните контактные данные для этой секции
- В Theme Options заполните общие данные для header/footer
- Это даст гибкость: можно иметь разные контакты в секции и в подвале

**Вариант 2:** Использовать только Theme Options
- Если контакты везде одинаковые, можно использовать только Theme Options
- Но тогда нужно изменить код компонентов, чтобы они брали данные из Options

**Текущая реализация:** Тема использует оба набора полей для максимальной гибкости.

---

## ❓ Вопрос 2: Почему изменения в полях не отображаются на странице?

### Возможные причины и решения

#### Причина 1: Страница не использует правильный шаблон

**Проверка:**
1. Откройте страницу "Главная" для редактирования
2. В правой панели найдите **"Атрибуты страницы"** → **"Шаблон"**
3. Убедитесь, что выбран шаблон **"Homepage"** (или "Template: Homepage")

**Решение:**
- Если шаблон не выбран или выбран другой, выберите "Homepage"
- Нажмите **"Обновить"** страницу

---

#### Причина 2: Группы полей не привязаны к правильной странице/шаблону

**Проверка:**
1. Перейдите: **Настройки → Secure Custom Fields → Группы полей**
2. Откройте каждую группу полей (hero_section, introduction_section и т.д.)
3. Проверьте раздел **"Привязка"** (Location Rules)

**Должно быть:**
- Для групп 1-13 (hero_section, introduction_section, contact_section и т.д.):
  - **Шаблон страницы** → **Homepage** (или конкретная страница "Главная")
  
- Для группы 14 (theme_options):
  - **Options Page** → **Theme Options** (или название вашей Options Page)

**Решение:**
- Если привязка неправильная, измените её на правильную
- Сохраните группу полей

---

#### Причина 3: Поля не сохранены

**Проверка:**
1. Откройте страницу "Главная" для редактирования
2. Прокрутите вниз до SCF полей
3. Измените какое-то поле
4. **Обязательно нажмите кнопку "Обновить"** (Update) вверху страницы

**Решение:**
- Всегда нажимайте "Обновить" после изменения полей
- Не закрывайте страницу без сохранения

---

#### Причина 4: Кеш браузера

**Проверка:**
- Откройте сайт в режиме инкогнито
- Или очистите кеш браузера (Ctrl+Shift+Delete)

**Решение:**
- Очистите кеш браузера
- Используйте жесткую перезагрузку: **Ctrl+F5** (Windows) или **Cmd+Shift+R** (Mac)

---

#### Причина 5: Кеш WordPress (если используется плагин кеширования)

**Проверка:**
- Проверьте, установлен ли плагин кеширования (WP Super Cache, W3 Total Cache, WP Rocket и т.д.)

**Решение:**
- Очистите кеш WordPress через плагин кеширования
- Или временно отключите плагин кеширования для проверки

---

#### Причина 6: Неправильные названия полей

**Проверка:**
1. Откройте группу полей в SCF
2. Проверьте, что **"Название поля"** (Field Name) точно совпадает с тем, что указано в инструкции
3. Например, для Hero Section должно быть:
   - `hero_name` (не `hero-name`, не `heroName`, не `Hero Name`)

**Важно:** Названия полей чувствительны к регистру и должны быть точно такими, как в инструкции!

**Решение:**
- Переименуйте поля, если названия не совпадают
- Используйте только строчные буквы и подчеркивания: `hero_name`, `contact_phone`

---

#### Причина 7: Поля не заполнены

**Проверка:**
1. Откройте страницу "Главная" для редактирования
2. Прокрутите вниз до SCF полей
3. Убедитесь, что поля действительно заполнены (не пустые)

**Решение:**
- Заполните все необходимые поля
- Для Repeater полей убедитесь, что добавлены строки (нажата кнопка "Добавить строку")

---

#### Причина 8: Плагин SCF не активирован

**Проверка:**
1. Перейдите: **Плагины → Установленные плагины**
2. Найдите "Secure Custom Fields"
3. Убедитесь, что плагин **активирован** (должна быть кнопка "Деактивировать")

**Решение:**
- Если плагин не активирован, нажмите "Активировать"

---

#### Причина 9: Проблема с получением данных в коде

**Проверка:**
1. Откройте главную страницу сайта
2. Нажмите **F12** (открыть DevTools)
3. Перейдите на вкладку **Console**
4. Проверьте, есть ли ошибки JavaScript или PHP

**Решение:**
- Если есть ошибки, проверьте логи WordPress
- Убедитесь, что все файлы темы загружены на сервер

---

### Пошаговая диагностика

Выполните эти шаги по порядку:

#### Шаг 1: Проверка шаблона страницы

1. Откройте страницу "Главная" для редактирования
2. В правой панели: **Атрибуты страницы** → **Шаблон**
3. Должно быть выбрано: **"Homepage"**
4. Если нет - выберите и нажмите **"Обновить"**

#### Шаг 2: Проверка привязки групп полей

1. **Настройки → Secure Custom Fields → Группы полей**
2. Откройте группу `hero_section`
3. Проверьте раздел **"Привязка"**:
   - Должно быть: **Шаблон страницы** → **Homepage**
4. Повторите для всех групп (1-13)

#### Шаг 3: Проверка названий полей

1. Откройте группу `hero_section`
2. Проверьте первое поле:
   - **Название поля** должно быть: `hero_name` (точно так, без пробелов, строчными буквами)
3. Если названия не совпадают - переименуйте поля

#### Шаг 4: Проверка заполнения полей

1. Откройте страницу "Главная" для редактирования
2. Прокрутите до группы `hero_section`
3. Заполните поле `hero_name` тестовым значением: `ТЕСТ`
4. Нажмите **"Обновить"** страницу
5. Откройте главную страницу сайта (не админ-панель)
6. Проверьте, изменилось ли имя в Hero секции

#### Шаг 5: Проверка кеша

1. Очистите кеш браузера
2. Сделайте жесткую перезагрузку: **Ctrl+F5**
3. Проверьте снова

---

### Быстрая проверка работоспособности

**Тест 1: Проверка получения данных**

1. Откройте страницу "Главная" для редактирования
2. В поле `hero_name` введите: `ТЕСТОВОЕ ИМЯ`
3. Нажмите **"Обновить"**
4. Откройте главную страницу сайта
5. Должно отображаться "ТЕСТОВОЕ ИМЯ" вместо "Екатерина Шулятникова"

**Если не работает:**
- Проверьте шаблон страницы (должен быть "Homepage")
- Проверьте привязку группы `hero_section` (должна быть к Homepage)
- Проверьте название поля (должно быть `hero_name`)

**Тест 2: Проверка Theme Options**

1. Откройте: **Настройки → Secure Custom Fields → Theme Options** (или ваша Options Page)
2. В поле `site_host_name` введите: `ТЕСТ ИМЕНИ`
3. Сохраните
4. Откройте сайт
5. В шапке (header) должно отображаться "ТЕСТ ИМЕНИ"

**Если не работает:**
- Проверьте, что Options Page создана
- Проверьте привязку группы `theme_options` к Options Page
- Проверьте название поля (должно быть `site_host_name`)

---

### Частые ошибки в названиях полей

❌ **Неправильно:**
- `hero-name` (дефис вместо подчеркивания)
- `Hero Name` (пробелы и заглавные буквы)
- `heroName` (camelCase)
- `hero_name_` (лишнее подчеркивание в конце)

✅ **Правильно:**
- `hero_name` (строчные буквы, подчеркивание)
- `contact_phone` (строчные буквы, подчеркивание)
- `site_host_name` (строчные буквы, подчеркивание)

---

### Проверка через код (для разработчиков)

Если у вас есть доступ к файлам темы, проверьте:

1. Откройте файл: `portfolio-theme/template-parts/components/hero-section.php`
2. Найдите строку: `$hero_name = ekaterina_get_scf_field( 'hero_name', ...`
3. Убедитесь, что название поля в коде (`'hero_name'`) совпадает с названием в SCF

**Если не совпадает:**
- Либо измените название поля в SCF
- Либо измените код (не рекомендуется, если не уверены)

---

### Контрольный список для диагностики

Используйте этот список для проверки:

- [ ] Страница "Главная" использует шаблон "Homepage"
- [ ] Все группы полей (1-13) привязаны к шаблону "Homepage"
- [ ] Группа `theme_options` привязана к Options Page
- [ ] Все названия полей точно совпадают с инструкцией (строчные, подчеркивания)
- [ ] Поля заполнены (не пустые)
- [ ] Страница "Главная" сохранена (нажата кнопка "Обновить")
- [ ] Плагин SCF активирован
- [ ] Кеш браузера очищен
- [ ] Кеш WordPress очищен (если используется)
- [ ] Нет ошибок в консоли браузера (F12)

---

### Если ничего не помогло

1. **Проверьте логи ошибок WordPress:**
   - Обычно находятся в `/wp-content/debug.log`
   - Или включите отладку в `wp-config.php`: `define( 'WP_DEBUG', true );`

2. **Проверьте права доступа к файлам:**
   - Файлы: 644
   - Директории: 755

3. **Проверьте версию SCF:**
   - Убедитесь, что используется актуальная версия плагина

4. **Временно отключите другие плагины:**
   - Возможно, конфликт с другим плагином

5. **Проверьте версию PHP:**
   - Должна быть PHP 8.1 или выше

---

## 📞 Дополнительная помощь

Если проблема не решена:

1. Сделайте скриншоты:
   - Настройки группы полей (привязка)
   - Страница редактирования с заполненными полями
   - Результат на сайте

2. Проверьте:
   - Версию WordPress
   - Версию плагина SCF
   - Версию PHP

3. Включите отладку WordPress и проверьте логи ошибок

